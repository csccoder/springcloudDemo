server:
  port: 8081

spring:
  application:
    name: GOODS

eureka:
  instance:
    instance-id: goods-api-8081
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/  # 注册中心访问地址